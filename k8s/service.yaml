apiVersion: v1
kind: Service
metadata:
  name: langraph-agent
  labels:
    app: langraph-agent
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: langraph-agent
